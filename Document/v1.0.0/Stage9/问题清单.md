# Stage9 问题清单

> **状态**：✅ 全部关闭（2026-06-11）— 设计阶段问题已解答，可分发开发任务。
> **本文件用途**：记录 Stage9 设计 / 实现过程中需要用户决策的问题。

---

## 依赖的 Version 级问题（均已关闭）

| Version Q# | 决策 | 对 Stage9 的影响 |
|-----------|------|----------------|
| Q10 | SimpleDemo 独立 CMake target | Stage9 仍只编辑 `examples/SimpleDemo/`，不污染 SDK |
| Q14 | macOS 仅编译通过 | Stage9 验收同样仅在 Windows / Linux 真机执行 |
| Q3  | 默认 dynamic SDK | Demo 链接 `AudioEQ` shared 库，无需改 SDK 编译方式 |
| Q16 | 字体以字节数组嵌入库 | 9D 字体 atlas 修复需遵循该决策 |

---

## Stage9 内追加的问题（全部已关闭）

| # | 问题 | 答案 | 来源 |
|---|------|------|------|
| S9-Q1 | 字体 atlas 当前未生成（`VulkanFontAtlas: No embedded font data available`），坐标轴文字不渲染——是否在本 Stage 一并修复？ | **A. 在 Stage9 内修** → 拉一个 SubStage 9D 出来负责修字体 atlas | 调试观察 |
| S9-Q2 | `setBandQ` 传入超出当前 `qRange` 的值，SDK 行为是 clamp 还是 reject（返回 `InvalidParameter`）？UI 端策略要据此选择 | **A. 先观察 SDK 行为，按现状实现** — 9B 实现时跑一次实测把观察到的行为登记到《开发指南》"SDK 实测行为表"；UI 不主动 clamp，调用 setter 后把 `ResultCode` 上报 StatusBar | 9B 实现细节 |
| S9-Q3 | `SampleRate` 枚举包含 `SR_192000`，但 Stage6 文档只列了 44.1/48/96。Stage9 是否要在采样率 ComboBox 加入 192k？ | **加入 192k**——既然枚举有就要演示 | 设计文档默认 |
| S9-Q4 | Add Band 表单提交后是否自动把焦点切到新 band？ | **是**——便于用户立即微调新 band | 设计文档默认 |
| S9-Q5 | Bypass All CheckBox 取消勾选时，是把所有 band 都置 bypass=false，还是恢复用户原本的勾选状态？ | **B. 记忆原始状态恢复** — 勾选时拍下当前每个 band 的 bypass 数组到本地缓存，取消勾选时按数组逐项 `setBandBypass` 还原；解勾期间用户手动改单个 band 的 bypass 不影响这块缓存 | 9A 实现细节 |
| S9-Q6 | 颜色按钮上的色样如何展示？`QPushButton` 自带 icon 是否够用？ | 用 `setIcon(QIcon(QPixmap::fromImage(单色)))`——足够，无需自绘 | 设计文档默认 |

---

## 关闭记录

| 问题编号 | 关闭时间 | 关闭说明 |
|---------|---------|---------|
| S9-Q1 | 2026-06-11 | 用户答 A — 字体 atlas 修复并入 Stage9，作为 9D |
| S9-Q2 | 2026-06-11 | 用户答 A — 不主动 clamp；9B 实测后把 SDK 现状写入开发指南 |
| S9-Q3 | 2026-06-11 | 设计阶段确认：4 档采样率全列 |
| S9-Q4 | 2026-06-11 | 设计阶段确认：Add 后自动 focus |
| S9-Q5 | 2026-06-11 | 用户答 B — 记忆原始状态恢复 |
| S9-Q6 | 2026-06-11 | 设计阶段确认：复用 QPushButton::setIcon |
