# Stage6 设计文档

> **Stage**：Stage6 — SimpleDemo 示例程序  
> **前置 Stage**：Stage5（必须完整完成）  
> **本 Stage 完成后进入**：版本收尾（全量系统测试）  
> **并行 SubStage**：只有 6A 一个 SubStage  
> **全局技术决策**：遵循 [总览.md](../总览.md#全局技术决策所有-stage-必须遵守)。SimpleDemo 为**独立可执行文件**，单独 CMake target（Q10）。macOS 仅保证编译通过（Q14）。

---

## 背景与目标

Stage5 完成了完整的 `AudioEQ` Widget。Stage6 实现 `examples/SimpleDemo`：一个独立可运行的 Qt 应用，演示 AudioEQ 的主要功能，用于：
- 手动验收：人工检查渲染效果和交互行为
- 作为 Stage8 兼容性测试的宿主程序

---

## 当前代码状态（Stage6 开始时）

- `src/AudioEQ.h/.cpp`：**已完整实现**（Stage5 产出）
- `examples/SimpleDemo/main.cpp`：**存在但为空桩**（Stage1 创建占位）
- `examples/SimpleDemo/CMakeLists.txt`：**已创建**（Stage1，目标名 `SimpleDemo`）

---

## SubStage 6A：SimpleDemo

### 目标
实现 `examples/SimpleDemo/main.cpp`，创建一个窗口，展示 `AudioEQ` 的全部主要功能。

### 布局和 UI 要求

```
┌─────────────────────────────────────────────────┐
│  [采样率: 44.1k] [48k] [96k]  [带宽数量: 3 5 8] │
│  [LPF: 开/关]  [HPF: 开/关]                      │
├─────────────────────────────────────────────────┤
│                                                  │
│              AudioEQ Widget                      │
│         （占据窗口大部分空间）                     │
│                                                  │
└─────────────────────────────────────────────────┘
```

工具栏使用 `QToolBar` 或简单 `QWidget` + `QPushButton`/`QComboBox`。

### 必须演示的功能

1. **多采样率切换**：3 个按钮（44.1kHz / 48kHz / 96kHz），切换后 X 轴上限变化可见
2. **Band 数量调整**：`setBandCount(3)` / `setBandCount(5)` / `setBandCount(8)`
3. **LPF/HPF 开关**：togglebutton，点击后手柄显示/隐藏，曲线更新
4. **拖拽交互**：无需额外代码，`AudioEQ` 本身已支持

### main.cpp 结构

```cpp
#include <QApplication>
#include <QMainWindow>
#include <QToolBar>
#include <QPushButton>
#include "AudioEQ.h"

int main(int argc, char* argv[]) {
    QApplication app(argc, argv);

    if (!AudioEQ::isVulkanSupported()) {
        qCritical("Vulkan is not supported on this device.");
        return 1;
    }

    QMainWindow window;
    window.setWindowTitle("AudioEQ SimpleDemo");
    window.resize(900, 500);

    auto* eq = new AudioEQ(&window);
    window.setCentralWidget(eq);

    // 工具栏
    auto* toolbar = window.addToolBar("Controls");
    // 采样率按钮
    auto* btn44k = new QPushButton("44.1kHz");
    auto* btn48k = new QPushButton("48kHz");
    auto* btn96k = new QPushButton("96kHz");
    QObject::connect(btn44k, &QPushButton::clicked, [eq]{ eq->setSampleRate(SampleRate::SR_44100); });
    QObject::connect(btn48k, &QPushButton::clicked, [eq]{ eq->setSampleRate(SampleRate::SR_48000); });
    QObject::connect(btn96k, &QPushButton::clicked, [eq]{ eq->setSampleRate(SampleRate::SR_96000); });
    toolbar->addWidget(btn44k);
    toolbar->addWidget(btn48k);
    toolbar->addWidget(btn96k);
    toolbar->addSeparator();
    // Band 数量
    auto* btn3 = new QPushButton("3 Bands");
    auto* btn5 = new QPushButton("5 Bands");
    auto* btn8 = new QPushButton("8 Bands");
    QObject::connect(btn3, &QPushButton::clicked, [eq]{ eq->setBandCount(3); });
    QObject::connect(btn5, &QPushButton::clicked, [eq]{ eq->setBandCount(5); });
    QObject::connect(btn8, &QPushButton::clicked, [eq]{ eq->setBandCount(8); });
    toolbar->addWidget(btn3);
    toolbar->addWidget(btn5);
    toolbar->addWidget(btn8);
    toolbar->addSeparator();
    // LPF / HPF
    auto* btnLpf = new QPushButton("LPF");
    btnLpf->setCheckable(true);
    QObject::connect(btnLpf, &QPushButton::toggled, [eq](bool on){ eq->setLpfEnabled(on); });
    auto* btnHpf = new QPushButton("HPF");
    btnHpf->setCheckable(true);
    QObject::connect(btnHpf, &QPushButton::toggled, [eq](bool on){ eq->setHpfEnabled(on); });
    toolbar->addWidget(btnLpf);
    toolbar->addWidget(btnHpf);

    window.show();
    return app.exec();
}
```

### 验收标准
- 程序启动，窗口显示，无崩溃
- 可见：背景深灰色、频率网格线、增益网格线、5 条曲线（初始均为 0dB 水平线）、5 个圆形 Band Handle
- 拖拽任意 Band Handle 后曲线实时变化
- 采样率按钮切换后 X 轴最高频率变化
- LPF/HPF 按钮切换后手柄出现/消失，曲线更新
- Band 数量按钮切换后 Handle 数量同步

---

## 完成门禁

- [ ] `cmake --build build` 后 `SimpleDemo` 可执行文件存在
- [ ] 运行 SimpleDemo：无崩溃，UI 正常显示
- [ ] 上述 6 条验收标准全部通过（人工检视）
- [ ] Vulkan validation 无 error 输出
