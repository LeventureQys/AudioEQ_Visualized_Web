# v2.0.0 Version 级问题清单

> **状态**：✅ **全部关闭（A1–H2 共 32 题）** — 已具备进入 Stage 1 实际开发的条件
> **记录人**：MainAgent
> **版本**：v2.0.0
> **创建日期**：2026-06-15
> **关闭日期**：2026-06-15

---

## 已确认的架构基线（无需再次确认）

以下决策已在 [README.md](./README.md) 与本次对话中明确，不需要再次回答：

| # | 项目 | 结论 |
|---|------|------|
| 1 | 组件形态 | 原生 Custom Element（`<audio-eq>`） |
| 2 | 渲染技术 | Canvas 2D |
| 3 | 实现语言 | 纯 JavaScript，不涉及任何 C++ 后台 |
| 4 | 线程模型 | 主线程同步计算，v2.0.0 不引入 Web Worker |
| 5 | 视觉目标 | 对齐 [../v1.0.0/目标样式.png](../v1.0.0/目标样式.png) |

---

## A 类 · 构建与工程基线（A1–A5）

### A1. 包管理器与构建工具？
**问题**：v2.0.0 选用什么前端工具链？

- [ ] A. 纯 ESM 零构建（直接 `<script type="module">` 即可用，源文件直发）
- [ ] B. Vite（开发体验最好，npm 包发布最方便）
- [ ] C. Rollup（专注库打包，输出体积小）
- [ ] D. Webpack（生态最广但配置最复杂）

**背景**：影响项目骨架、构建命令、产物布局，决定 Stage 1A 的工作量。

**MainAgent 推荐**：B（Vite）—— 开发体验最优，发布 ESM/UMD/IIFE 都开箱即用，零依赖项目也能享受 HMR 与 dev server。备选 A（纯零构建），如果用户希望最大化"通用组件"分发简单度。

**回复**
B


---

### A2. 输出产物格式？
**问题**：发布到 npm / GitHub Release 时提供哪些产物？

- [ ] A. 仅 ESM（`.mjs` 或 `.js` + `"type": "module"`）
- [ ] B. ESM + UMD（兼顾 npm 用户与 `<script>` 直接引入用户）
- [ ] C. ESM + UMD + IIFE（再加一份立即调用版本，浏览器直链最简）
- [ ] D. ESM + CommonJS（兼容老 Node 项目）

**背景**：影响 Stage 1A 的构建配置 + Stage 6B 的发布文档。

**MainAgent 推荐**：B —— ESM 是主流，UMD 兜底 `<script>` 用户；CommonJS 在浏览器组件场景几乎用不到。

**回复**
B


---

### A3. 浏览器最低支持版本？
**问题**：v2.0.0 承诺的浏览器兼容范围？

- [ ] A. Chromium-only（Chrome / Edge 最近 2 个版本）
- [ ] B. Evergreen 全套（Chrome / Edge / Firefox 最近 2 个版本，**不含 Safari**）
- [ ] C. Evergreen + Safari 15+（Custom Element v1、Pointer Events、ResizeObserver 全支持）
- [ ] D. 兼容 IE11（需 polyfill 大量 API，工作量倍增）

**背景**：影响是否需要 polyfill（Pointer Events、ResizeObserver、Custom Element 等）。Safari 15 发布于 2021-09，目前市占率 > 95%。

**MainAgent 推荐**：C —— 现代浏览器全覆盖，零 polyfill。

**回复**

C

---

### A4. 是否允许第三方运行时依赖？
**问题**：v2.0.0 的运行时依赖政策？

- [ ] A. 零运行时依赖（`dependencies: {}`，只有 devDependencies）
- [ ] B. 允许 < 30KB 的 tiny libs（如 `mitt` 事件库、`tween.js` 缓动）
- [ ] C. 不限制（用户自行评估）

**背景**：Canvas 2D 与 Custom Element 都是浏览器原生 API，理论上零依赖完全可行。

**MainAgent 推荐**：A —— 零依赖最适合"通用组件"，避免依赖地狱与版本冲突。

**回复**
A


---

### A5. 是否生成 `.d.ts` 类型声明？
**问题**：v2.0.0 不写 TS 源码，但是否给 TS 用户提供类型提示？

- [ ] A. 不生成 `.d.ts`（不针对 TS 用户）
- [ ] B. 通过 JSDoc 注释 + `tsc --emitDeclarationOnly` 自动生成 `.d.ts`
- [ ] C. 手写一份 `index.d.ts` 维护类型

**背景**：用户已确认"纯 JS"，但 `.d.ts` 是发布产物而非源码，不违反"纯 JS"。B 方案让源文件继续是 `.js`，只是带 JSDoc 注释，发布时附带类型声明。

**MainAgent 推荐**：B —— 是"纯 JS"约束下兼顾 TS 用户体验的最优解。

**回复**
B


---

## B 类 · 组件契约与 API 形态（B1–B6）

### B1. Custom Element 标签名？
**问题**：HTML 中使用的标签名？

- [ ] A. `<audio-eq>`（简短直观）
- [ ] B. `<aeq-equalizer>`（带项目前缀避免冲突）
- [ ] C. `<x-audio-eq>`（带 `x-` 前缀的实验/扩展惯例）
- [ ] D. 其他（请写出）

**背景**：Custom Element 规范要求标签名含连字符。一旦定下，全部文档与 Demo 都使用。

**MainAgent 推荐**：A —— 简短易记，与项目名直接对应。`<audio-eq>` 标签在 npmjs.com 与 GitHub 检索无明显冲突。

**回复**
A


---

### B2. Attribute ↔ Property 同步策略？
**问题**：HTML attribute（`sample-rate="48000"`）与 JS property（`element.sampleRate = 48000`）的同步行为？

- [ ] A. 完全双向同步（标准 Custom Element 行为：attribute change → property change → 反向同步）
- [ ] B. 仅 attribute → property 单向
- [ ] C. 仅 property，不识别 attribute
- [ ] D. attribute 只做初始化值，运行时只看 property

**背景**：影响 `attributeChangedCallback` 和 setter 的实现复杂度。完全双向需要小心避免无限循环。

**MainAgent 推荐**：A —— 与标准 Custom Element / 大多数 HTML 内置元素行为一致；初始化场景可用 attribute，动态更新用 property。

**回复**
A


---

### B3. 事件命名风格？
**问题**：组件向外抛出的 CustomEvent 命名风格？

- [ ] A. 全小写短横线（`band-changed` / `band-dragged` / `focused-band-changed`）
- [ ] B. camelCase（`bandChanged` / `bandDragged`）
- [ ] C. 全大写下划线（`BAND_CHANGED`）

**背景**：DOM 事件惯例是全小写（`click` / `mouseover`），但短横线也很常见（`focus-trap-entered`）。

**MainAgent 推荐**：A —— 与 DOM 事件最自然贴合，HTML 模板里写 `onband-changed` 也最直观。

**回复**
A


---

### B4. 错误反馈机制？
**问题**：v1.0.0 用 `ResultCode` 枚举返回错误。v2.0.0 怎么做？

- [ ] A. 沿用 ResultCode 字符串枚举（如 `"IndexOutOfRange"`），方法返回该字符串或 `"OK"`
- [ ] B. 直接 throw Error（JS 惯例），保留 ResultCode 名作为 `Error.code` 字段
- [ ] C. 返回 `{ ok: boolean, code?: string, message?: string }`
- [ ] D. 静默失败 + 通过事件抛出 `error` 事件

**背景**：JS 惯例倾向 throw；但调用方批量调用 API 时 throw 会打断序列。

**MainAgent 推荐**：B —— `throw new Error('Index out of range', { cause: { code: 'IndexOutOfRange' } })`，调用方按需 try/catch；保留语义化 code 便于 i18n 和判断。

**回复**
B


---

### B5. 算法核心是否独立可 import？
**问题**：除了 `<audio-eq>`，是否同时导出底层模块？

- [ ] A. 仅暴露 Custom Element，所有逻辑封装在内部
- [ ] B. 同时导出 `AudioEQCore`（含 ButterworthIIR / CurveEngine / CoordinateMapper），供 headless（无 UI）用户使用
- [ ] C. 仅导出算法（ButterworthIIR）+ Custom Element，不导出 CurveEngine / Mapper

**背景**：算法本身是纯函数模块，开放成本接近零。某些用户可能只想拿算法做服务端 EQ 计算或自定义 UI。

**MainAgent 推荐**：B —— 给"我只想要曲线计算"用户一条路径，扩大组件适用场景。

**回复**
B


---

### B6. 多实例支持？
**问题**：同一页面是否允许多个 `<audio-eq>` 共存？

- [ ] A. 允许（每个实例独立 state，互不影响）
- [ ] B. 仅单例（第二个实例报错或被忽略）

**背景**：Custom Element 天然支持多实例，禁止反而要额外代码。

**MainAgent 推荐**：A —— 零成本，且符合 DOM 直觉。

**回复**
A


---

## C 类 · 渲染细节（C1–C5）

### C1. DPR (devicePixelRatio) 适配策略？
**问题**：高 DPI 屏幕的处理？

- [ ] A. 自动监听 `window.devicePixelRatio` 变化（用户跨屏拖窗口时自适配）
- [ ] B. 仅初始化时读一次（用户跨屏需手动 reload）
- [ ] C. 通过 attribute 显式控制（`dpr="2"`）
- [ ] D. 不做 DPR 适配（Canvas 用 CSS 像素）

**背景**：未做 DPR 适配的 Canvas 在 Retina 屏会模糊。监听 DPR 变化需 `matchMedia('(resolution: ...)')`。

**MainAgent 推荐**：A —— 用户跨屏自动适配，体验最好。

**回复**

A

---

### C2. 抗锯齿策略？
**问题**：曲线、网格、文字的抗锯齿如何处理？

- [ ] A. 仅依赖 Canvas2D 默认 AA（配合 DPR 缩放，lineWidth ≥ 1.5 视觉足够好）
- [ ] B. 主曲线用多遍半透明描边模拟 smoothstep 边缘
- [ ] C. 离屏 Canvas 双倍尺寸渲染再缩放（成本高但效果最好）

**背景**：Canvas2D 默认 AA 质量取决于浏览器，但配 DPR=2 缩放后通常视觉 OK。

**MainAgent 推荐**：A —— 简单可靠，先做 A，若视觉不达标再升级到 B。

**回复**

A

---

### C3. 曲线平滑算法？
**问题**：CurveEngine 输出 ~500 个离散点，如何画成平滑曲线？

- [ ] A. 直线段 `lineTo`（500 个点本身已足够密）
- [ ] B. `quadraticCurveTo`（每三个相邻点取中位作控制点）
- [ ] C. 手算 Catmull-Rom 样条细分后 lineTo
- [ ] D. `bezierCurveTo` 三阶贝塞尔

**背景**：v1.0.0 GPU 端用 Catmull-Rom 细分；500 个点配 Canvas2D 默认 AA 可能 A 也够。

**MainAgent 推荐**：A —— 简单可靠；500 个点已经接近每像素 1 个点，视觉差异肉眼难辨。若 A 不够再升级。

**回复**
A


---

### C4. 文字渲染？
**问题**：坐标轴文字、Band 内数字、LPF/HPF 字样如何渲染？

- [ ] A. Canvas2D `fillText` 直接绘（每帧都画，文字内容变了直接重绘）
- [ ] B. 预渲染图集（光栅化所有字符到离屏 Canvas，类似 v1.0.0 的 stb_truetype 方案）
- [ ] C. DOM 文字叠加在 Canvas 上层（可访问性最好）

**背景**：v1.0.0 用 stb_truetype + 字体图集，是因为 GPU 渲染文字需要纹理。Canvas2D 没这个限制。

**MainAgent 推荐**：A —— Canvas2D `fillText` 直接够用，省去字体图集一整套模块；DPR 缩放下文字也清晰。

**回复**

A

---

### C5. 帧率与重绘调度？
**问题**：什么时机触发重绘？

- [ ] A. 任何变更立即 `requestAnimationFrame`
- [ ] B. 拖拽时 60Hz 节流；空闲时 dirty flag 控制（只在数据变化时重绘一次）
- [ ] C. 固定 60Hz 持续重绘（即使没变化）

**背景**：B 最节能；C 简单但浪费 CPU/GPU/电量。

**MainAgent 推荐**：B —— rAF 自然 60Hz，配 dirty flag 让组件在静态时完全停止。

**回复**

B

---

## D 类 · 视觉与默认值（D1–D4）

### D1. 颜色基线沿用哪版？
**问题**：曲线色 + 背景色的默认值？

- [ ] A. v1.0.0 原版（曲线 `#00FF00` 绿 + 背景 `#1A1A1A`）
- [ ] B. v1.0.1 修正版（曲线 `#F0A030` 橙黄 + 背景 `#262A33` 深蓝灰）
- [ ] C. 与目标样式.png 对齐（橙黄曲线 + 深色背景）

**背景**：用户提供的目标样式.png 显示橙黄色曲线。

**MainAgent 推荐**：C（与 B 等价）—— 用户已在 v1.0.1 调整为橙黄+深色，且目标样式.png 也是此风格。

**回复**

C

---

### D2. 默认增益范围？
**问题**：Y 轴默认显示范围？

- [ ] A. v1.0.0 默认 `[-48, +48]` dB
- [ ] B. v1.0.1 / 目标图默认 `[-24, +12]` dB（不对称）
- [ ] C. `[-24, +24]` dB（对称，常见的 6dB 倍数）
- [ ] D. `[-18, +18]` dB

**背景**：不对称范围在 EQ 软件中较少见，但目标样式.png 确实显示 -24 到 +12。

**MainAgent 推荐**：B —— 与目标样式.png 一致，对外提供 setter 允许覆盖。

**回复**

B

---

### D3. 字体方案？
**问题**：组件内文字的字体？

- [ ] A. 系统默认 sans-serif（`font-family: system-ui, sans-serif`）
- [ ] B. 嵌入 Noto Sans WebFont（base64 编码进 JS，带 OFL 许可证文件）
- [ ] C. 不指定（继承父元素 font-family）
- [ ] D. 让用户通过 CSS variable 配置

**背景**：v1.0.0 嵌入 Noto Sans 是因为 Vulkan 需要 .ttf 文件做光栅化。Canvas2D 没此限制。

**MainAgent 推荐**：A —— 零依赖最简，跨平台视觉一致；用户需要自定义时可通过 CSS variable（见 D4）。

**回复**

A

---

### D4. 哪些视觉属性可对外覆盖？
**问题**：用户可改的视觉项？

- [ ] A. 全部通过 CSS variables（`--audio-eq-curve-color`、`--audio-eq-bg-color`、`--audio-eq-grid-color` 等）
- [ ] B. 仅曲线色 + 背景色，通过 attribute（`curve-color="#00FF00"` 与 `background-color="#000"`）
- [ ] C. 全锁定，不提供外部覆盖
- [ ] D. CSS variables + 关键项也提供 attribute

**背景**：CSS variables 适合视觉主题；attribute 适合属性反射。两者可叠加。

**MainAgent 推荐**：D —— 默认主题硬编码（视觉一致），关键 2-3 项（曲线色 / 背景色 / Band 色）暴露 attribute；其他视觉细节走 CSS variables。

**回复**

D

---

## E 类 · 数据模型移植（E1–E4）

### E1. 默认 band 数量与分布？
**问题**：`new AudioEQ()` 后初始有几个 band？

- [ ] A. 沿用 v1.0.0 默认 5 个，对数均匀分布 `[20Hz, 20kHz]`
- [ ] B. 0 个，需用户显式 `addBand()`
- [ ] C. 改为目标图.png 的 12 个
- [ ] D. 默认 5 个，但分布改为更密集（如 31Hz/63Hz/125Hz/250Hz/...）

**背景**：v1.0.0 spec 第 21 条明确 5 个；目标图 12 个是 Demo 演示。

**MainAgent 推荐**：A —— SDK 默认应保守，调用方自行 addBand。

**回复**

A

---

### E2. LPF/HPF 的 index 表示？
**问题**：v1.0.0 用 magic number（LPF=-1，HPF=-2）。v2.0.0 怎么做？

- [ ] A. 沿用 magic number（-1 / -2）
- [ ] B. 改为字符串 ID（`"lpf"` / `"hpf"`）作为可选 index 类型
- [ ] C. 完全独立 API（`setLpfFrequency(f)` / `setHpfEnabled(b)` 等），不混 index

**背景**：JS 不像 C++ 严格类型，magic number 容易出错。

**MainAgent 推荐**：C —— 独立 API 最清晰；同时仍保留 `lpf` / `hpf` 字段在 model 上作为可枚举的特殊 band。

**回复**
C


---

### E3. Band index 删除后是否复用？
**问题**：`removeBand(2)` 后下次 `addBand()` 的 index？

- [ ] A. 复用最小空闲 index（v1.0.0 行为，index 2 会被复用）
- [ ] B. 紧凑重排（后续 index 全部 -1，list 始终密集）
- [ ] C. 单调递增（始终用更大的 index，旧 index 永不复用）

**背景**：A 节省内存但 index 不是有意义的"id"；B 会破坏调用方持有的 index 引用；C 内存可能耗尽（实际场景不太可能）。

**MainAgent 推荐**：A —— 与 v1.0.0 行为一致，调用方需注意 index 可能复用。

**回复**

A

---

### E4. 默认采样率？
**问题**：构造时的初始采样率？

- [ ] A. 沿用 v1.0.0 默认 44100 Hz
- [ ] B. 改为 48000 Hz（目标样式.png 用 48k）
- [ ] C. 96000 Hz
- [ ] D. 由 attribute 必填，无默认值

**背景**：Web Audio API 默认 48k；CD 标准 44.1k。

**MainAgent 推荐**：B —— 48k 更贴合 Web 场景，且目标图也是 48k。

**回复**

B

---

## F 类 · 工程纪律（F1–F3）

### F1. 测试框架？
**问题**：单元测试 + 集成测试用什么？

- [ ] A. Vitest（Vite 生态原生，启动快）
- [ ] B. Jest（生态最广，但配置 ESM 较麻烦）
- [ ] C. Mocha + Chai
- [ ] D. 无单元测试，仅人工验收 Demo

**背景**：依赖 A1 的选择。若 A1 选 Vite，F1 选 Vitest 最顺。

**MainAgent 推荐**：A —— 与 A1 推荐的 Vite 配对最佳。

**回复**

A

---

### F2. 代码风格工具？
**问题**：代码风格 / 静态检查工具链？

- [ ] A. Prettier + ESLint（自动格式化 + 错误检查）
- [ ] B. 仅 Prettier
- [ ] C. 仅 ESLint
- [ ] D. 不引入任何工具

**背景**：多人协作必备。零配置也比无强。

**MainAgent 推荐**：A —— 标配。Prettier 管格式，ESLint 管错误。

**回复**

A

---

### F3. CI/CD？
**问题**：自动化流程？

- [ ] A. GitHub Actions 跑 lint + 测试 + 自动发包到 npm
- [ ] B. GitHub Actions 仅跑 lint + 测试，发包人工触发
- [ ] C. 无 CI

**背景**：v2.0.0 还在 alpha 阶段，自动发包风险偏高。

**MainAgent 推荐**：B —— 测试自动化，发包人工把关，对小项目最稳。

**回复**


B
---

## G 类 · 扩展与极端场景（G1–G3）

### G1. SSR / Node 环境兼容？
**问题**：当 `typeof window === 'undefined'`（Node / SSR 预渲染）时？

- [ ] A. 检测环境降级为只读 stub（Custom Element 不注册，但 `AudioEQCore` 算法模块依然可用）
- [ ] B. 完全不考虑 SSR（在 Node 环境 import 直接报错）
- [ ] C. 仅算法模块兼容 Node，Custom Element 部分报警告

**背景**：Next.js / Nuxt 用户会在 SSR 阶段 import 组件。完全不考虑会让这些用户额外做 dynamic import。

**MainAgent 推荐**：A —— 低成本兼容（仅一个 `typeof window` 守卫），让 SSR 用户开箱即用。

**回复**

A

---

### G2. Viewport resize 响应方式？
**问题**：组件容器尺寸变化时如何感知？

- [ ] A. `ResizeObserver` 自动监听（组件自管理）
- [ ] B. `window.resize` 事件监听（只能感知窗口级 resize，无法感知容器级）
- [ ] C. 仅显式 `.resize(w, h)` 方法（调用方负责）

**背景**：组件可能在 flex / grid 布局中被父容器拉伸，`window.resize` 抓不到。

**MainAgent 推荐**：A —— 标准答案，零成本自管理。

**回复**

A

---

### G3. 性能上限验收标准？
**问题**：v2.0.0 的性能硬指标？

- [ ] A. 10 个 band × 60Hz 拖拽时单帧总耗时 < 16ms（含计算 + 渲染）
- [ ] B. 5 个 band × 60Hz 拖拽时 < 16ms（更宽松）
- [ ] C. 不设硬指标，仅要求"主观流畅"

**背景**：v1.0.0 spec 提到"典型使用 ≤10"，但没设硬性指标。

**MainAgent 推荐**：A —— 设硬指标可在 Stage 7C 集成测试中机器验证，避免后期被动优化。500 点 × 10 band 在现代浏览器主线程 < 1ms，留出 15ms 给渲染应当宽裕。

**回复**
A


---

## H 类 · 视觉迁移细节（H1–H2）

### H1. 视觉边距 / 坐标轴布局？
**问题**：沿用 v1.0.0 Stage10 / Stage11 的"回字形边距 + L 形轴 + 0dB 高亮 + 文字基线对齐"设计？

- [ ] A. 完全沿用 Stage10 + Stage11 的视觉设计，对齐目标样式.png
- [ ] B. 重新设计（请说明）

**背景**：v1.0.0 Stage10 已设计回字形边距（内口/外口边距 + 文字标签搬到边距），Stage11 已设计配色 / 网格 / 文字布局。这些设计与目标样式.png 一致。

**MainAgent 推荐**：A —— 视觉规范已经过用户审核，直接沿用。

**回复**

A

---

### H2. 频率刻度档位（X 轴 ticks）？
**问题**：X 轴显示哪些频率档？

- [ ] A. v1.0.1 修正版 10 档（含 1kHz）：`20 / 50 / 100 / 200 / 500 / 1k / 2k / 5k / 10k / 20k` Hz
- [ ] B. v1.0.0 原版 9 档（不含 1kHz）：`20 / 50 / 100 / 200 / 500 / 2k / 5k / 10k / 20k` Hz
- [ ] C. 重新设计

**背景**：目标样式.png 显示的是 v1.0.1 的 10 档版本。

**MainAgent 推荐**：A —— 与目标样式.png 一致。

**回复**

A

---

## 开发中追加问题

（此区域在开发过程中由 MainAgent 追加。SubAgent 遇到的任何不确定性必须先报告 MainAgent，由 MainAgent 追加到此处并提交用户解答。）

---

## 问题关闭记录

> 用户对每一题答复后，MainAgent 在此表记录最终决策。本表全部填写后视为问题清单关闭。

| 问题编号 | 状态 | 最终决策摘要 |
|---------|------|-------------|
| A1 | ✅ 关闭 | Vite 作为构建工具 |
| A2 | ✅ 关闭 | 输出 ESM + UMD 两套产物 |
| A3 | ✅ 关闭 | Evergreen + Safari 15+，零 polyfill |
| A4 | ✅ 关闭 | 零运行时依赖（`dependencies: {}`） |
| A5 | ✅ 关闭 | JSDoc 注释 + `tsc --emitDeclarationOnly` 自动生成 `.d.ts` |
| B1 | ✅ 关闭 | Custom Element 标签名 `<audio-eq>` |
| B2 | ✅ 关闭 | Attribute ↔ Property 完全双向同步（标准 CE 行为） |
| B3 | ✅ 关闭 | 事件名全小写短横线（`band-changed` 等） |
| B4 | ✅ 关闭 | 错误用 `throw new Error(msg, { cause: { code } })`；保留语义化 code |
| B5 | ✅ 关闭 | 同时导出 `AudioEQCore`（ButterworthIIR / CurveEngine / CoordinateMapper / EqualizerModel） |
| B6 | ✅ 关闭 | 允许同一页面多个 `<audio-eq>` 实例 |
| C1 | ✅ 关闭 | 自动监听 `window.devicePixelRatio` 变化 |
| C2 | ✅ 关闭 | 仅依赖 Canvas2D 默认 AA，先做 A，不达标再升级 |
| C3 | ✅ 关闭 | 直线段 `lineTo`（500 点已足够密） |
| C4 | ✅ 关闭 | Canvas2D `fillText` 直接绘 |
| C5 | ✅ 关闭 | 拖拽 rAF 60Hz 节流；空闲态 dirty flag 控制 |
| D1 | ✅ 关闭 | 颜色基线与目标样式.png 对齐（橙黄曲线 + 深色背景） |
| D2 | ✅ 关闭 | 默认增益范围 `[-24, +12]` dB（与目标图一致），setter 可覆盖 |
| D3 | ✅ 关闭 | 字体 `system-ui, sans-serif` |
| D4 | ✅ 关闭 | 默认硬编码主题；关键 2-3 项暴露 attribute；其他走 CSS variables |
| E1 | ✅ 关闭 | 默认 5 个 band，对数均匀分布 `[20Hz, 20kHz]` |
| E2 | ✅ 关闭 | LPF/HPF 完全独立 API（`setLpfFrequency()` 等），不混 band index |
| E3 | ✅ 关闭 | Band 删除后复用最小空闲 index（与 v1.0.0 一致） |
| E4 | ✅ 关闭 | 默认采样率 48000 Hz |
| F1 | ✅ 关闭 | 测试框架 Vitest |
| F2 | ✅ 关闭 | Prettier + ESLint |
| F3 | ✅ 关闭 | GitHub Actions 跑 lint + 测试，发包人工触发 |
| G1 | ✅ 关闭 | SSR 兼容：`typeof window === 'undefined'` 时降级为只读 stub，算法模块仍可用 |
| G2 | ✅ 关闭 | `ResizeObserver` 自动监听容器尺寸变化 |
| G3 | ✅ 关闭 | 性能验收：10 band × 60Hz 拖拽单帧 < 16ms |
| H1 | ✅ 关闭 | 沿用 v1.0.0 Stage10/Stage11 视觉设计 |
| H2 | ✅ 关闭 | X 轴频率刻度 10 档（含 1kHz） |

**关闭门禁**：✅ 全部 32 题已关闭，所有决策已写入 [总览.md](./总览.md) 的「全局技术决策」表，并形成 [开发计划.md](./开发计划.md)。可以进入 Stage 1 拆分。
